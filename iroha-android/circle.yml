machine:
  java:
    version: oraclejdk8
  environment:
    ANDROID_HOME: /usr/local/android-sdk-linux
    ANDROID_NDK_HOME: ${ANDROID_NDK}
    JAVA_OPTS: "-Xmx2048m -XX:MaxPermSize=1024m"
    GRADLE_OPTS: '-Dorg.gradle.jvmargs="-Xmx2048m -XX:+HeapDumpOnOutOfMemoryError"'
  post:
    - sudo service mysql stop
    - sudo service postgresql stop

dependencies:
  pre:
    # Android SDK Platform 25
    - if [ ! -d "/usr/local/android-sdk-linux/platforms/android-25" ]; then echo y | android update sdk --no-ui --all --filter "android-25"; fi
    # Android SDK Build-tools, revision 25.x.y
    - if [ ! -d "/usr/local/android-sdk-linux/build-tools/25.0.1" ]; then echo y | android update sdk --no-ui --all --filter "build-tools-25.0.2"; fi
    # Android Support Repository, revision 39 / Local Maven repository for Support Libraries
    - if [ ! -d "/usr/local/android-sdk-linux/extras/android/m2repository/com/android/support/design/25.0.2" ]; then echo y | android update sdk --no-ui --all --filter "extra-android-m2repository"; fi
    - echo y | android update sdk --no-ui --all --filter "extra-google-m2repository,extra-android-support,extra-google-google_play_services"
  cache_directories:
    - /usr/local/android-sdk-linux/platforms/android-25
    - /usr/local/android-sdk-linux/build-tools/25.0.2
    - /usr/local/android-sdk-linux/extras/android/m2repository
  override:
    - ./gradlew -v
    - ./gradlew dependencies

test:
  override:
    - echo "Nothing to do here"
#    - ./gradlew clean assembleProductionDebug check

general:
  artifacts:
    - "app/build/outputs/apk/app-production-release.apk"